---
title: {Мета-заголовок у формі неозначеного дієслова, наприклад: «Керувати транзакціями»}
description: {Мета-опис, наприклад: «Дізнайтеся, як переглядати, фільтрувати та скасовувати транзакції в кабінеті партнера»}
last_update:
  date: {дата у форматі m/d/yyyy, наприклад: 2/22/2026}
---

{/*
ОБЛАСТЬ ЗАСТОСУВАННЯ ШАБЛОНУ: Посібники користувача описують, як виконати задачу. Вони процедурні (покрокові), а не концептуальні. Для фонової інформації та контексту використовуйте шаблон концептуальної теми.

ПОЗНАЧЕННЯ ПЛЕЙСХОЛДЕРІВ:
- {curly_braces}        = плейсхолдери шаблону, які заповнює автор. Видаліть перед публікацією.
- *`ВЕЛИКИМИ_ЛІТЕРАМИ`* = плейсхолдери, що залишаються у фінальному документі — їх замінює читач (згідно зі стайлгайдом).

НЕЗАПОВНЕНИЙ КОНТЕНТ: Використовуйте {/* ToDo: ПОЯСНЕННЯ */} для відсутнього контенту чи фактів, що потребують підтвердження від SME.

ПРАВИЛА РІВНІВ ЗАГОЛОВКІВ:
- Один H2 на операцію або етап.
- Незалежні операції → ## Операції з {назва}, H3 для кожної операції.
- Послідовні етапи    → ## Етапи {назва}, H3 для кожного етапу.
- Довідкова інформація: H2 (спільна для всього посібника) або H3 під конкретною операцією, якщо поля різняться між операціями.

СКРІНШОТИ: Зберігайте у `./.assets/` поруч із документом. Посилайтеся: `![Описовий alt-текст](./.assets/{назва-скріншоту}.png)`.

ІКОНКИ В КРОКАХ: Для кнопок без текстової мітки використовуйте компонент `<Icon>` перед жирною назвою дії:
`<Icon icon="ic:sharp-edit" height="24" style={{ color: '#9564ff' }} /> **Редагувати**`.
Якщо елемент UI має текстову мітку — використовуйте лише жирний текст.
*/}

{Вступний абзац: коротко опишіть задачу і розділ кабінету партнера, до якого належить ця сторінка. Наприклад: «Цей гайд описує, як [задача] у розділі [назва розділу] кабінету партнера.»}

{/*
НЕОБОВ'ЯЗКОВИЙ РОЗДІЛ — КОРОТКО ПРО ПРОЦЕС.
Додайте, якщо сторінка описує цілісний бізнес-процес (наприклад, «Створити транзакцію», «Налаштувати вебхук»), де читачеві важливо зрозуміти контекст до виконання кроків.
Для простих операцій (наприклад, «Фільтрувати транзакції», «Переглянути деталі») — не додавайте.
*/}

## Коротко про процес

- **Тип дії:** {переказ / виведення / перегляд / налаштування тощо}
- **Де виконується:** {назва розділу кабінету партнера}
- **Ключова умова:** {що має бути виконано, перш ніж процес стане доступним}
- **Основний результат:** {що вважається успішним завершенням процесу}

{/*
НЕОБОВ'ЯЗКОВИЙ РОЗДІЛ — ЩО ПОТРІБНО ЗНАТИ ПЕРЕД ПОЧАТКОМ.
Додайте, якщо є бізнес-правила, обмеження або особливості, які читачеві важливо зрозуміти до початку роботи, але які не є формальними передумовами.
Не дублюйте тут пункти з розділу Передумови.
Приклади: «Транзакції у статусі `success` не можна скасувати», «Вебхук можна налаштувати лише один раз для одного проєкту».
*/}

## Що потрібно знати перед початком

- {Особливість або обмеження 1.}
- {Особливість або обмеження 2.}

## Передумови

{/*
Перелічуйте лише умови, спільні для всіх операцій або етапів цієї сторінки.
Передумови, специфічні для однієї операції, додавайте у блок **Додаткові передумови** в межах самої операції.
Якщо передумов немає — використайте стандартне речення (Варіант А або Варіант Б):

Варіант А (один розділ довідкової інформації на сторінці):
Перед початком ознайомтеся з [довідковою інформацією](#довідкова-інформація-{slug}) або звертайтеся до неї за потреби під час виконання процесу.

Варіант Б (кілька розділів довідкової інформації):
Кожен розділ містить довідкову інформацію. Ознайомтеся з нею перед початком або звертайтеся до неї під час виконання процесу. (Примітка: для Варіанта Б додайте inline-посилання на кожен розділ довідкової інформації в описі відповідної операції/етапу.)
*/}

Перед початком переконайтеся, що ви виконали такі передумови:

- {Передумова 1. Наприклад: «Ви увійшли в кабінет партнера.»}
- {Передумова 2. Наприклад: «Транзакція існує в системі.»}

{/*
НЕОБОВ'ЯЗКОВИЙ РОЗДІЛ — СТАТУСИ.
Додайте, якщо в межах цього посібника є статуси, які впливають на те, що партнер може зробити.
Перед таблицею завжди має бути вступне речення.
Якщо статусів немає — видаліть цей розділ.
Ви увійшли в кабінет партнера - цей пунт не згадуємо, він зрозумілий з конткексту.
*/}

## Статуси

{Вступне речення. Наприклад: «Транзакція може мати такі статуси:»}

| Статус | Де відображається | Що означає | Що може зробити партнер |
|---|---|---|---|
| `{статус_1}` | {де} | {значення} | {доступна дія або «Лише перегляд»} |
| `{статус_2}` | {де} | {значення} | {доступна дія або «Лише перегляд»} |

{/*
===========================================================================
ВАРІАНТ А — ОПЕРАЦІЇ (незалежні дії)

Використовуйте, якщо сторінка описує набір незалежних дій, які партнер може виконувати окремо
(наприклад, «Фільтрувати транзакції», «Переглянути деталі», «Скасувати транзакцію»).

ОДНА ОПЕРАЦІЯ: якщо сторінка описує лише одну операцію — НЕ використовуйте `## Операції з {назва}`
і НЕ додавайте Accordion. Пишіть одразу:

  ## {Назва операції}

  Щоб {ціль}, виконайте такі кроки:

  1. {Крок 1.}
  2. {Крок 2.}

  **Результат:** {Опис результату.}

Видаліть ВАРІАНТ Б, якщо використовуєте ВАРІАНТ А.
===========================================================================
*/}

## Операції з {назва_розділу}

{Вступне речення. Наприклад: «У меню **Транзакції** ви можете виконувати такі операції:»}

<Accordion titleAs="h3" title="{Операція 1}">

{/*
НЕОБОВ'ЯЗКОВИЙ БЛОК — ДОДАТКОВІ ПЕРЕДУМОВИ.
Додайте, якщо для цієї операції є умови, яких немає у загальному розділі Передумови.
Якщо таких умов немає — видаліть цей блок.
*/}

**Додаткові передумови:**

- {Передумова, специфічна для цієї операції.}

{/*
Якщо операція складається лише з одного кроку, оформлюйте її маркерованим пунктом (-), а не нумерованим списком.
Для мікроінструкцій усередині кроку дотримуйтеся порядку: спочатку ціль, потім дія.
Правильно: «Щоб застосувати фільтр, натисніть **Застосувати**.»
Неправильно: «Натисніть **Застосувати**, щоб застосувати фільтр.»

Об'єднуйте крок «знайдіть X» із наступним кроком «натисніть Y», якщо Y діє саме на той X. Місце вкажіть на початку кроку.
Правильно: «У списку квитанцій знайдіть потрібний файл і натисніть **Завантажити**.»
Неправильно: «Знайдіть потрібний файл у списку квитанцій.» + окремий крок «Натисніть **Завантажити**.»

Для кроків підтвердження в діалозі (де ціль — не сама операція, а лише підтвердження попередньої дії) — дія спочатку, причина в кінці:
Правильно: «У вікні **Видалення файлу** натисніть **Видалити**, щоб підтвердити дію.»
Неправильно: «Щоб підтвердити видалення, у вікні **Видалення файлу** натисніть **Видалити**.»
*/}

1. {Крок 1.}
2. {Крок 2.}

   ![{Описовий alt-текст}](./.assets/{назва-скріншоту}.png)

3. {Крок 3.}

**Результат:** {Короткий опис результату. Наприклад: «Транзакцію скасовано. Статус змінено на `cancelled`.»}

![{Описовий alt-текст результату}](./.assets/{назва-скріншоту-результату}.png)

{/*
НЕОБОВ'ЯЗКОВИЙ БЛОК — ДОДАТКОВІ ДІЇ.
Додайте, якщо після завершення основної операції партнер може виконати пов'язані дії, які не є обов'язковими для завершення цієї задачі.
*/}

**Додаткові дії:**

Після {назва_операції} ви також можете:

- **{Додаткова дія 1}:** {коротко, як виконати.}
- **{Додаткова дія 2}:** {коротко, як виконати.}

</Accordion>

<Accordion titleAs="h3" title="{Операція 2}">

1. {Крок 1.}
2. {Крок 2.}

   ![{Описовий alt-текст}](./.assets/{назва-скріншоту}.png)

**Результат:** {Опис результату.}

</Accordion>

<Accordion titleAs="h3" title="{Операція 3}">

- {Єдиний крок (маркер, не нумерований список).}

**Результат:** {Опис результату.}

</Accordion>

{/*
===========================================================================
ВАРІАНТ Б — ЕТАПИ (послідовний процес)

Використовуйте, якщо сторінка описує цілісний бізнес-процес, де кожен наступний крок
починається зі стану, що є результатом попереднього (наприклад, «Створити та надіслати
транзакцію», «Налаштувати інтеграцію через API»).

Правило поділу:
- Якщо весь процес ≤ 10 кроків — залиште один розділ (ВАРІАНТ А) без поділу на етапи.
- Якщо > 10 кроків або процес логічно розбивається на частини з паузами та перевірками —
  використовуйте ВАРІАНТ Б.

Видаліть ВАРІАНТ А, якщо використовуєте ВАРІАНТ Б.
===========================================================================
*/}

## Етапи {назва_процесу}

Щоб {висока_ціль_процесу}, пройдіть такі етапи:

<Accordion title="1. {Назва}">

{Вступний абзац: поясніть, де зараз перебуває партнер і що він робитиме на цьому етапі. Не починайте одразу зі списку кроків.}

{/*
Якщо для цього етапу є одна критична умова — подайте її одним реченням.
Якщо умов кілька — використовуйте вступне речення та список.
*/}

Перед цим етапом переконайтеся, що {умова}.

1. {Крок 1.}
2. {Крок 2.}

   ![{Описовий alt-текст}](./.assets/{назва-скріншоту}.png)

3. {Крок 3.}

**Результат:** {Опис стану після завершення етапу.} Перейдіть до **2. {Назва}**.

</Accordion>

<Accordion title="2. {Назва}">

{Вступний абзац.}

1. {Крок 1.}
2. {Крок 2.}

   ![{Описовий alt-текст}](./.assets/{назва-скріншоту}.png)

**Результат:** {Опис стану після завершення етапу.} Перейдіть до **3. {Назва}**.

</Accordion>

<Accordion title="3. {Назва}">

{Вступний абзац.}

1. {Крок 1.}
2. {Крок 2.}

**Результат:** {Опис фінального результату. Наприклад: «На цьому налаштування завершено.»}

</Accordion>

{/*
===========================================================================
КІНЕЦЬ ВАРІАНТІВ СТРУКТУРИ
===========================================================================
*/}

{/* Явний ID {#довідкова-інформація-{slug}} робить якір детермінованим; {slug} — kebab-case slug операції. Речення Варіанта А посилається на нього. Якщо розділів довідкової інформації кілька (Варіант Б), кожному дайте власний ID з його slug. */}
## Довідкова інформація {#довідкова-інформація-{slug}}

{/*
Цей розділ описує атрибути форм, полів або параметрів, з якими взаємодіє партнер.

Варіанти розміщення:
- Спільні поля для всіх операцій → залиште таблицю тут, у кінці.
- Поля різняться між операціями → перемістіть відповідну таблицю одразу під ту операцію, якої вона стосується, і видаліть цей загальний розділ.
- Операції без форм і полів → видаліть цей розділ.
*/}

{Вступне речення. Наприклад: «Нижченаведена таблиця описує атрибути, які ви вводите та вибираєте під час {операція}:»}

| Атрибут | Опис |
|---|---|
| {Назва атрибута} | {Опис атрибута.} |

## Наступні кроки

{/*
Вкажіть пов'язані посібники, до яких читач може перейти після виконання задач цієї сторінки.
Для суміжних концептуальних документів, що не є посібниками, використовуйте «Пов'язані документи» (розділ з концептуальної теми), а не цей розділ.
Якщо наступних кроків немає — видаліть цей розділ.
*/}

- [{Назва посібника}](/посилання/на/посібник.md)
- [{Назва посібника}](/посилання/на/посібник.md)

{/*
===========================================================================
DESIGN RULES — НЕ ВКЛЮЧАТИ У ФІНАЛЬНИЙ ДОКУМЕНТ
===========================================================================
*/}

## Design rules

### Вибір структури: «Операції» vs «Етапи»

- **Операції (ВАРІАНТ А)**: незалежні дії, кожна виконується самостійно (наприклад: фільтрувати транзакції, переглянути деталі, скасувати). Порядок — будь-який.
- **Етапи (ВАРІАНТ Б)**: послідовний процес, де результат одного кроку є початком наступного (наприклад: створити транзакцію → надіслати → отримати підтвердження). Порядок обов'язковий.
- Не використовуйте обидві структури на одній сторінці.
- Якщо весь процес ≤ 10 кроків — ВАРІАНТ А без поділу на етапи. Якщо > 10 кроків або є логічні паузи — ВАРІАНТ Б.

### Accordion — коли і як використовувати

- **Одна операція (ВАРІАНТ А):** Accordion не використовується. Пишіть `## {Назва операції}` з кроками напряму.
- **Кілька операцій (ВАРІАНТ А):** кожна операція — `<Accordion titleAs="h3" title="{Назва операції}">...</Accordion>`. Заголовок `## Операції з {назва}` зберігається.
- **Етапи (ВАРІАНТ Б):** кожен етап — `<Accordion title="N. {Назва}">...</Accordion>` (без `titleAs`). Жодного нумерованого списку назв перед блоками — лише вступне речення і одразу Accordion. Посилання на наступний етап — жирним: «Перейдіть до **2. {Назва}**.»
- Не використовуйте `### H3` заголовки для окремих операцій або етапів.

### Вступні речення до процедур

- **Вступне речення всередині Accordion:** якщо `title` вже передає дію (наприклад, «Додати квитанцію»), починайте одразу з кроків. Вступне речення потрібне лише якщо потрібен контекст, якого немає в заголовку.
- Якщо вступне речення все ж потрібне — пишіть звичайним (не жирним) текстом: `Щоб {ціль}, виконайте такі дії:`
- **Однокрокова процедура:** `Щоб {ціль}:` + маркерований пункт.
- **Список етапів:** `Щоб {висока_ціль}, пройдіть такі етапи:` (звичайний текст, не жирний) + нумерований список.
- Порядок: мета → дія. ✅ «Щоб застосувати фільтр, натисніть **Застосувати**.» ⛔ «Натисніть **Застосувати**, щоб застосувати фільтр.»

### Передумови

- У розділ **Передумови** — лише умови, спільні для **всіх** операцій/етапів сторінки.
- Умова, специфічна для однієї операції — у блок **Додаткові передумови** всередині тієї операції.
- «Ви увійшли в кабінет партнера» — **не включайте**: це само собою зрозуміло з контексту.
- Якщо передумова одна — одне речення без маркерів. Якщо кілька — вступне речення + маркерований список.

### Блок «Результат»

- Закриває кожну операцію або кожен етап; описує підсумковий стан, не повторює кроки.
- **Операції (ВАРІАНТ А):** незалежні, жодних посилань на «наступну операцію».
- **Етапи (ВАРІАНТ Б):** в усіх, крім останнього, додайте: «Тепер перейдіть до наступного кроку: [Етап X. {Назва}](#anchor).» В останньому: «На цьому {назва процесу} завершено.»

### Опціональні блоки

Не додавайте блок без змістовного наповнення:

| Блок | Коли додавати |
|---|---|
| Коротко про процес | Тільки для багатоетапних бізнес-процесів, де контекст важливий перед кроками |
| Що потрібно знати | Є бізнес-правила або обмеження, що не є формальними передумовами |
| Статуси | Статуси впливають на доступні дії партнера |
| Додаткові передумови | Є умови, специфічні для однієї конкретної операції |
| Додаткові дії | Є необов'язкові дії після завершення основної операції |
| Довідкова інформація | Є форми або поля, що потребують пояснення |
| Наступні кроки | Є пов'язані посібники, до яких читач природно перейде |

### Кроки процедури

- Крок = одна логічна дія користувача, не обов'язково один клік. «Знайдіть X» перед дією над тим самим X — це не окремий крок: об'єднуйте його з наступним кліком в один пункт списку. ✅ «У списку квитанцій знайдіть потрібний файл і натисніть **Завантажити**.» ⛔ «Знайдіть потрібний файл у списку квитанцій.» + окремий крок «Натисніть **Завантажити**.»
- Порядок усередині кроку: місце → дія. ✅ «У вікні **Квитанції** натисніть...» ⛔ «Натисніть... у вікні **Квитанції**.»
- Крок підтвердження в діалозі: дія → причина (протилежно до правила мета → дія, яке стосується головної цілі всієї операції, а не підтвердження вже виконаної дії). ✅ «У вікні **Видалення файлу** натисніть **Видалити**, щоб підтвердити дію.» ⛔ «Щоб підтвердити видалення, у вікні **Видалення файлу** натисніть **Видалити**.»
- Поза цими випадками — один крок, одна незалежна дія.
- Системна реакція («Форма відкривається») — в тому ж рядку після крапки, не окремим пунктом.
- Скріншот під кроком — лише якщо допомагає ідентифікувати елемент або підтвердити результат; не механічно після кожного кроку.
- **Голос:** не UI-елемент є суб'єктом дії, а дія відбувається з предметом. ✅ «Транзакції відображаються у таблиці». ⛔ «Таблиця відображає транзакції». Це стосується і блоків **Результат**.
- **Посилання на Довідкову інформацію** в кроках: додавайте inline-посилання безпосередньо в релевантній фразі кроку — не окремим реченням. ✅ «виберіть потрібні [фільтри](#довідкова-інформація-{slug})». ⛔ «Детальніше про кожен фільтр — у розділі **Довідкова інформація**». Якір має збігатися з явним ID `{#довідкова-інформація-{slug}}` цільового заголовка; якщо розділів кілька, посилайтеся на конкретний slug.

### Скріншоти

- Зберігайте в `./.assets/` поруч із документом.
- Додавайте, якщо: (1) підтверджує результат, (2) допомагає знайти складний елемент UI, (3) ілюструє неочевидний екран.
- **Два синтаксиси вставки — обирайте за класифікацією (скіл є джерелом правил):**
  - **Повноекранний** (повне меню, дашборд або таблиця на всю ширину контентної зони): `![Описовий alt-текст](./.assets/{назва-скріншоту}.png)`
  - **Компактний** (діалогове вікно, модал або вузька панель, що займає значно менше повної ширини): `<img src={require('./.assets/{назва-скріншоту}.png').default} width="480" alt="Описовий alt-текст" />`

### Довідкова інформація (атрибути форм)

- Спільні поля для всіх операцій → таблиця в загальному `## Довідкова інформація` наприкінці.
- Поля різняться між операціями → таблиця одразу під відповідною операцією; загальний розділ видаліть.
- Назву атрибута пишіть **точно як в UI**, без форматування.

### Термінологія та форматування

- Терміни: з глосарію `${CLAUDE_PLUGIN_ROOT}/context/doc-rules/project-rules/glossary-ua.md`.
- UI-мітки та UI-статуси (як відображаються в інтерфейсі): **жирним** без лапок: натисніть **Зберегти**; статус **Завершений**.
- Системні значення API (не видимі в UI як текст): `code font`: `success`, `cancelled`; **UK spelling**: `cancelled` (не `canceled`).
- Плейсхолдери для читача: `*`\``ВЕЛИКИМИ_ЛІТЕРАМИ`\``*`.
- Перша згадка абревіатури: повна форма + скорочення: «API-токен (Application Programming Interface)»; далі — лише скорочення.
