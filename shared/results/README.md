Put your test reports here.

After running the tool on your Mac, copy the report it made (from the section's `.sources/` folder) into this folder, rename it so we can tell them apart (e.g. `transactions-readiness.json`), add a note if something looked wrong, then commit and push.

See ../README.md for the full steps.
